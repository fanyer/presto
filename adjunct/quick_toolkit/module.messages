MSG_QUICKTK_CLOSE_CONTEXT			arjanl

	Close a certain context, will lead to its destruction
	First parameter (MH_PARAM_1) should be a pointer to the UiContext
	that has to be closed.

MSG_DIM_PAGEVIEW					emil

	Dim or undim pageview of this DesktopWindow.

MSG_QUICKTK_RELAYOUT				arjanl

	Request a relayout of a UI element.
