MSG_MEDIA_BACKENDS_GST_STATE_CHANGED		andersr

	Message posted to the main thread when the shared state was changed by
	a delegate thread.

	Depends on: MEDIA_BACKEND_GSTREAMER

MSG_MEDIA_BACKENDS_GST_SRC				philipj

	Message posted to the main thread to make it feed data to
	the GStreamer source element.

	Depends on: MEDIA_BACKEND_GSTREAMER

MSG_MEDIA_BACKENDS_GST_THREAD_MANAGER			philipj

	Message posted for GstMediaManager to start pending threads.

	Depends on: MEDIA_BACKEND_GSTREAMER
