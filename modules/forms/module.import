API_URL_HTTP_POST_DATA			bratell

	This module needs multipart upload functionality from the upload module

	Import if	: always

API_LIBSSL_ASYNC_KEYGEN			bratell

        If we have keygen support, we need the keygen API:s

        Import if: FEATURE_NATIVE_SSL, !defined(SSL_DISABLE_CLIENT_CERTIFICATE_INSTALLATION)

API_LIBSSL_CERTSINSTALL_CLASS		temporary_I_think

        To get the keygen stuff, we don't really need these API:s but it doesn't compile otherwise

	Import if: FEATURE_NATIVE_SSL

API_FORMATS_URI_ESCAPE_SUPPORT                  roarl

        Use format's escaping API in TempBuffer8

        Import if       : always

API_WF2_CODE_ALWAYS_ON		bratell

        Make sure the WEBFORMS2_SUPPORT define is enabled.

        Import if       : always
