API_SVG_GET_PRESENTATIONAL_ATTRIBUTES		andersr

	Expose SVGManager::GetPresentationalAttributes, which allows
	callers to acquire a list of CSS_decl objects from the
	presentational attributes.

	Defines   : SVG_GET_PRESENTATIONAL_ATTRIBUTES
	Depends on: FEATURE_SVG

API_SVGPATH_TO_VEGAPATH			fs

	API for converting a SVGPath (as returned by
	OpFont::GetOutline) into a VEGAPath.

	Defines   : SVG_PATH_TO_VEGAPATH
	Depends on: FEATURE_SVG
