group "util.language_code_conversions";

language c++;

include "modules/util/language_codes.h";

test("Test language code conversion")
{
    OpString three_letter_code;
    OP_STATUS error;
    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ab"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("abk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ace"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ace")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ach"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ach")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ada"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ada")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ady"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ady")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("aa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("aar")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("afh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("afh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("af"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("afr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("afa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("afa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ain"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ain")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ak"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("aka")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("akk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("akk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sq"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("alb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gsw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gsw")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ale"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ale")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("alg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("alg")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tut"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tut")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("am"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("amh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("anp"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("anp")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("apa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("apa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ar"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ara")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("an"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("arg")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("arp"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("arp")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("arw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("arw")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hy"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("arm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("rup"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("rup")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("art"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("art")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("as"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("asm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ast"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ast")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ath"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ath")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("aus"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("aus")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("map"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("map")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("av"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ava")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ae"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ave")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("awa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("awa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ay"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("aym")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("az"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("aze")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ban"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ban")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bat"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bat")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bal"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bal")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bm"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bam")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bad"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bad")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bnt"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bnt")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bas"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bas")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ba"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bak")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("eu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("baq")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("btk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("btk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bej"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bej")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("be"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bel")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bem"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bem")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ben")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ber"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ber")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bho"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bho")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bih")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bik"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bik")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("byn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("byn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bin"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bin")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bis")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zbl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zbl")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nob")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bs"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bos")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bra"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bra")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("br"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bre")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bug"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bug")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bul")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bua"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bua")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("my"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bur")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cad"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cad")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("es"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("spa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ca"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cat")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cau"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cau")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ceb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ceb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cel"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cel")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("km"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("khm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chg")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cmc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cmc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ch"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cha")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ce"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("che")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ny"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nya")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chy"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chy")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chp"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chp")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cho"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cho")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("za"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zha")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chu")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chv")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nwc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nwc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("syc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("syc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("rar"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("rar")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cop"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cop")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cor")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("co"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cos")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cre")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mus"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mus")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("crp"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("crp")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cpe"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cpe")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cpf"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cpf")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cpp"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cpp")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("crh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("crh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hrv")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cus"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cus")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cs"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("cze")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dak"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dak")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("da"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dan")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dar"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dar")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("del"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("del")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("div")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zza"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zza")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("din"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("din")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("doi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("doi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dgr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dgr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dra"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dra")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dua"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dua")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dut")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dum"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dum")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dyu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dyu")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dz"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dzo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("frs"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("frs")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("efi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("efi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("egy"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("egy")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("eka"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("eka")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("elx"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("elx")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("en"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("eng")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("enm"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("enm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ang"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ang")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("myv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("myv")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("eo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("epo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("et"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("est")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ee"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ewe")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ewo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ewo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fan"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fan")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fat"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fat")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fao")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fj"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fij")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fil"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fil")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fin")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fiu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fiu")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fon"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fon")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fre")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("frm"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("frm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fro"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fro")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fur"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fur")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ff"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ful")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gaa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gaa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gd"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gla")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("car"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("car")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("glg")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lug")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gay"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gay")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gba"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gba")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gez"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gez")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ka"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("geo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("de"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ger")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nds"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nds")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gmh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gmh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("goh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("goh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gem"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gem")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ki"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kik")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gil"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gil")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gon"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gon")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gor"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gor")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("got"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("got")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("grb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("grb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("grc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("grc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("el"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gre")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kal")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("grn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("guj")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gwi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gwi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ht"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hat")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ha"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hau")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("haw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("haw")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("he"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("heb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hz"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("her")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hil"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hil")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("him"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("him")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hin")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ho"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hmo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hit"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hit")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hmn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hmn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hun")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hup"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hup")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("iba"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("iba")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("is"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ice")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("io"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ido")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ig"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ibo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ijo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ijo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ilo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ilo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("arc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("arc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("smn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("smn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("inc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("inc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ine"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ine")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("id"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ind")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("inh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("inh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ia"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ina")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ie"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ile")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("iu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("iku")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ik"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ipk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ira"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ira")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ga"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("gle")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mga"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mga")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sga"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sga")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("iro"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("iro")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("it"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ita")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ja"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("jpn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("jv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("jav")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kac"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kac")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("jrb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("jrb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("jpr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("jpr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kbd"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kbd")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kab"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kab")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("xal"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("xal")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kam"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kam")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kan")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kau")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pam"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pam")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kaa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kaa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("krc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("krc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("krl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("krl")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kar"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kar")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ks"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kas")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("csb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("csb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kaw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kaw")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kaz")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kha"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kha")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("khi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("khi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kho"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kho")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kmb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kmb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("rw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kin")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ky"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kir")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tlh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tlh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kom")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kon")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kok"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kok")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ko"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kor")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kos"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kos")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kpe"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kpe")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kro"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kro")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kj"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kua")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kum"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kum")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ku"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kur")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kru"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kru")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("kut"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("kut")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lad"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lad")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lah"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lah")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lam"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lam")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("day"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("day")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lao")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("la"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lat")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lav")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ltz")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lez"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lez")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("li"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lim")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ln"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lin")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lt"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lit")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("jbo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("jbo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("dsb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("dsb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("loz"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("loz")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lub")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lua"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lua")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lui"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lui")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("smj"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("smj")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lun"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lun")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("luo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("luo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lus"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lus")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mac")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mad"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mad")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mag"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mag")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mak"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mak")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mlg")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ms"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("may")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ml"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mal")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mt"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mlt")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mnc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mnc")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mdr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mdr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("man"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("man")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mni"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mni")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mno"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mno")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("gv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("glv")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mao")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("arn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("arn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mar")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("chm"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("chm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mah")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mwr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mwr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mas"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mas")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("myn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("myn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("men"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("men")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mic"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mic")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("min"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("min")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mwl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mwl")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("moh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("moh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mdf"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mdf")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ro"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("rum")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mkh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mkh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("lol"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("lol")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mon")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mos"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mos")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mul"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mul")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mun"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mun")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nqo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nqo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nah"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nah")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("na"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nau")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nav")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nd"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nde")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nbl")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ng"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ndo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nap"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nap")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("new"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("new")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ne"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nep")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nia"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nia")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nic"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nic")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ssa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ssa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("niu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("niu")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zxx"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zxx")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nog"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nog")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("non"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("non")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("frr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("frr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("se"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sme")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nso"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nso")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("no"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nor")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nno")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nub"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nub")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ii"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("iii")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nym"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nym")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nyo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nyo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("nzi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("nzi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("oc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("oci")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pro"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pro")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("oj"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("oji")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("or"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ori")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("om"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("orm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("osa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("osa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("os"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("oss")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("oto"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("oto")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pal"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pal")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pau"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pau")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pli")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pag"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pag")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pan")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pap"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pap")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("paa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("paa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ps"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pus")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("per")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("peo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("peo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("phi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("phi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("phn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("phn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pon"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pon")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pol")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pt"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("por")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("pra"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("pra")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("qu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("que")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("raj"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("raj")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("rap"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("rap")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("qaa-qtz"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("qaa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("roa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("roa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("rm"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("roh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("rom"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("rom")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("rn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("run")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ru"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("rus")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sal"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sal")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sam"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sam")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("smi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("smi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sm"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("smo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sad"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sad")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sag")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("san")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sat"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sat")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sc"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("srd")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sas"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sas")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sco"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sco")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sel"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sel")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sem"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sem")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("srp")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("srr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("srr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("shn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("shn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sna")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("scn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("scn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sid"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sid")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sgn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sgn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bla"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("bla")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sd"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("snd")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("si"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sin")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sit"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sit")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sio"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sio")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sms"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sms")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("den"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("den")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sla"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sla")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("slo")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("slv")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sog"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sog")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("so"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("som")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("son"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("son")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("snk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("snk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("wen"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("wen")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("st"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sot")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("alt"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("alt")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sma"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sma")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("srn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("srn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("suk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("suk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sux"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sux")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("su"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sun")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sus"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sus")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("swa")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ss"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ssw")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("swe")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("syr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("syr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tgl")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ty"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tah")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tg"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tgk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tmh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tmh")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ta"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tam")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tt"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tat")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("te"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tel")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ter"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ter")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tet"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tet")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("th"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tha")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("bo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tib")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tig"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tig")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ti"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tir")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tem"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tem")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tiv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tiv")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tli"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tli")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tpi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tpi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tkl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tkl")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tog"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tog")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("to"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ton")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tsi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tsi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ts"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tso")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tn"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tsn")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tum"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tum")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tup"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tup")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tr"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tur")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ota"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ota")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tuk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tvl"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tvl")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tyv"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("tyv")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("tw"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("twi")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("udm"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("udm")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("uga"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("uga")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ug"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("uig")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("uk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ukr")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("umb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("umb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("mis"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("mis")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("und"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("und")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("hsb"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("hsb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ur"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("urd")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("uz"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("uzb")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("vai"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("vai")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ve"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ven")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("vi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("vie")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("vo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("vol")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("vot"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("vot")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("wak"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("wak")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("wa"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("wln")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("war"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("war")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("was"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("was")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("cy"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("wel")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("fy"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("fry")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("wal"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("wal")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("wo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("wol")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("xh"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("xho")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("sah"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("sah")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("yao"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("yao")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("yap"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("yap")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("yi"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("yid")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("yo"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("yor")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("ypk"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("ypk")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("znd"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("znd")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zap"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zap")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zen"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zen")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zu"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zul")));

    three_letter_code.Empty();
    error = LanguageCodeUtils::ConvertTo3LetterCode(&three_letter_code, UNI_L("zun"));
    verify(OpStatus::IsSuccess(error));
    verify(uni_str_eq(three_letter_code.CStr(), UNI_L("zun")));

}
