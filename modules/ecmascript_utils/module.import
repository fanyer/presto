API_HC_OPERA_RUNSLICE                           jl

        Used to reenter the message loop while executing
        ECMAScript synchronously.

        Import if: API_ESUTILS_SYNC_INTERFACE
        Import if: API_ESUTILS_SYNC_EVAL
        Import if: API_ESUTILS_SYNC_CALL
        Import if: API_ESUTILS_SYNC_PROPERTIES

API_ESUTILS_ES_ENVIRONMENT                      jl

        Used by this module's selftest suite to test other, more
        fundamental, facilities, and is therefore imported if
        FEATURE_SELFTEST is enabled.

        Is also imported, as a fallback, if FEATURE_ES_ENVIRONMENT is
        enabled until that feature has gone away and users have been
        updated to import this API instead.

        Import if: FEATURE_SELFTEST
        Import if: FEATURE_ES_ENVIRONMENT

API_ESUTILS_ASYNC_EVAL                          jl

        Import the API for testing in core-gogi, if selftest is enabled.

        Import if: FEATURE_SELFTEST, CORE_GOGI

API_ESUTILS_ASYNC_CALLFUNCTION                  jl

        Import the API for testing in core-gogi, if selftest is enabled.

        Import if: FEATURE_SELFTEST, CORE_GOGI

API_ESUTILS_ASYNC_CALLMETHOD                    jl

        Import the API for testing in core-gogi, if selftest is enabled.

        Import if: FEATURE_SELFTEST, CORE_GOGI

API_ESUTILS_ASYNC_PROPERTIES                    jl

        Import the API for testing in core-gogi, if selftest is enabled.

        Import if: FEATURE_SELFTEST, CORE_GOGI

API_ESUTILS_SYNC_EVAL                           jl

        Import the API for testing in core-gogi, if selftest is enabled.

        Import if: FEATURE_SELFTEST, CORE_GOGI

API_ESUTILS_SYNC_CALL                           jl

        Import the API for testing if selftest is enabled.

        Import if: FEATURE_SELFTEST, CORE_GOGI

API_ESUTILS_SYNC_PROPERTIES                     jl

        Import the API for testing if selftest is enabled.

        Import if: FEATURE_SELFTEST, CORE_GOGI

API_PI_SOCKET_LISTEN                            jl

        Needed for remote non-scope debugger to connect.

        Import if: ECMASCRIPT_REMOTE_DEBUGGER
