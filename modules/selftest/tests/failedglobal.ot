group "selftest.norequire";
require NOT_DEFINED;

test("Should not happen") 
{
	verify(0);
}

global 
{
  static not_a_type selftest_not_really_data[100] ={};
  extern godis polkagris();
  void funky()
  {
     syntax error here we go.
  };
  syntax error in global section. 
  Should not matter.
}
