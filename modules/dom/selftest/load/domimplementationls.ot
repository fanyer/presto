group "DOM.Load.DOMImplementationLS.basic";
require init;
require DOM3_LOAD;

language ecmascript;

test("DOMImplementationLS constants")
{
  verify(DOMImplementationLS.MODE_SYNCHRONOUS == 1);
  verify(DOMImplementationLS.MODE_ASYNCHRONOUS == 2);
}
