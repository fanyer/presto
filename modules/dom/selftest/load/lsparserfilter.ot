group "DOM.Load.LSParserFilter.basic";
require init;
require DOM3_LOAD;

language ecmascript;

test("LSParserFilter constants")
{
  verify(LSParserFilter.FILTER_ACCEPT == 1);
  verify(LSParserFilter.FILTER_REJECT == 2);
  verify(LSParserFilter.FILTER_SKIP == 3);
  verify(LSParserFilter.FILTER_INTERRUPT == 4);
}
