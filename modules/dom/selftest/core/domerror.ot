group "DOM.Core.DOMError";
require init;
language ecmascript;

html "";

test("DOMError constants")
{
  verify(DOMError.SEVERITY_WARNING == 1);
  verify(DOMError.SEVERITY_ERROR == 2);
  verify(DOMError.SEVERITY_FATAL_ERROR == 3);
}
