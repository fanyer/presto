group "DOM.Save.XMLSerializer";
require init;
require DOM3_SAVE;

language ecmascript;

test("XMLSerializer exists")
{
  verify(window.XMLSerializer);
}

test("XMLSerializer.prototype exists")
{
  verify(window.XMLSerializer);
  verify(XMLSerializer.prototype);
}
