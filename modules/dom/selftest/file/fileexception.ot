// -*- Mode: c++; tab-width: 4; indent-tabs-mode: t; c-basic-offset: 4 -*-

group "DOM.File.FileException";
require init;

require DOM_WEBWORKERS_SUPPORT;

language ecmascript;

html "";

/* Tested in filereadersync.ot */
test("FileException")
{
	// They are in workers, not in a document.
	verify(!("FileException" in window));
}
